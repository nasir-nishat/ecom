# Build-time env for the public demo on Cloudflare Workers (`pnpm build:cf`, loaded via --mode cloudflare).
# Committed on purpose: nothing secret here. Runtime vars live in wrangler.jsonc.
PUBLIC_SITE_URL=https://ecom.nasirnishat.com
PUBLIC_SITE_NAME=Agentic Store
PUBLIC_DEMO_MODE=on
PUBLIC_SUPABASE_ANON_KEY=
